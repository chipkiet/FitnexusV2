# For Docker
POSTGRES_USER=fitnexus_user
POSTGRES_PASSWORD=your_secret_password
POSTGRES_DB=fitnexus_db
POSTGRES_PORT=5433
POSTGRES_HOST=localhost

# For Express App
PORT=3001
NODE_ENV=development


JWT_SECRET=315b52e92465b909657f7f3e98b2a55b58f6f881a0c39cd625ff3eacf343fefb
JWT_EXPIRES_IN=7d

FRONTEND_URL=http://localhost:5173

# trong môi trường dev ( chạy 2 cổng khác nhau), frontend sẽ cần gọi API backend
# tạo VITE_BACKEND_URL
VITE_BACKEND_URL=http://localhost:3001

# Gemini (Google Generative AI)
GEMINI_API_KEY=AIzaSyBlKW7WJ9F4LOFy6webPTjgUMFBfLYzqms

# Python AI Trainer service (FastAPI) endpoint
# Default: http://127.0.0.1:8000/analyze-image/
AI_API_URL=http://127.0.0.1:8000/analyze-image/

# AI usage quota (guest/Free plan)
# Enable quota enforcement (1 on, 0 off)
AI_QUOTA_ENABLED=1
# Weekly limit per feature for FREE/guest
AI_QUOTA_FREE_LIMIT=5
# Salt for hashing anonymous callers (do not use a secret here; override in real env)
AI_QUOTA_SALT=change_me

# PayOS
# Get your credentials from https://dashboard.payos.vn/
# Set PAYOS_ENV to "sandbox" for testing, or "production" for live payments
PAYOS_ENV=sandbox
PAYOS_CLIENT_ID=your_payos_client_id
PAYOS_API_KEY=your_payos_api_key
PAYOS_CHECKSUM_KEY=your_payos_checksum_key

